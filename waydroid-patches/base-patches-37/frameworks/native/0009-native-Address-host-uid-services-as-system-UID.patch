From e98c61d30ad2520bf02fcf0dae867011b7afd54c Mon Sep 17 00:00:00 2001
From: Erfan Abdi <erfangplus@gmail.com>
Date: Fri, 18 Jul 2025 13:42:02 +0000
Subject: [PATCH 09/14] native: Address host uid services as system UID

Change-Id: If534b9a36f40f15390f5b5a6ca726ebf267406c1
Signed-off-by: SupeChicken666 <me@supechicken666.dev>
---
 libs/binder/Android.bp         | 1 +
 libs/binder/IPCThreadState.cpp | 6 ++++++
 2 files changed, 7 insertions(+)

diff --git a/libs/binder/Android.bp b/libs/binder/Android.bp
index b3caaae..1b0a5a3 100644
--- a/libs/binder/Android.bp
+++ b/libs/binder/Android.bp
@@ -375,6 +375,7 @@ cc_defaults {
     },
 
     shared_libs: [
+        "libbase",
         "libcutils",
         "liblog",
         "libutils",
diff --git a/libs/binder/IPCThreadState.cpp b/libs/binder/IPCThreadState.cpp
index 6c4dd46..a028cae 100644
--- a/libs/binder/IPCThreadState.cpp
+++ b/libs/binder/IPCThreadState.cpp
@@ -35,6 +35,7 @@
 #include <chrono>
 #endif // PCC_LOGGING
 
+#include <android-base/properties.h>
 #include <utils/CallStack.h>
 #include <utils/SystemClock.h>
 
@@ -448,6 +449,11 @@ status_t IPCThreadState::clearLastError()
 pid_t IPCThreadState::getCallingPid() const
 {
     checkContextIsBinderForUse(__func__);
+    if (mCallingUid != 1000) {
+        int hostuid = android::base::GetIntProperty("waydroid.host.uid", 1000);
+        if (mCallingUid == (uid_t)hostuid)
+            return 1000;
+    }
     return mCallingPid;
 }
 
-- 
2.55.0


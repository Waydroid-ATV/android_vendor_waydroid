From cfe18069f6e4f213915f3b32ec6d6e79dc82cfe1 Mon Sep 17 00:00:00 2001
From: SupeChicken666 <me@supechicken666.dev>
Date: Sat, 28 Feb 2026 17:35:34 +0800
Subject: [PATCH 18/28] init: Do not try to force unmount /data on reboot

Otherwise, the host's rootfs will also be unmounted

Signed-off-by: SupeChicken666 <me@supechicken666.dev>
---
 init/reboot.cpp | 2 ++
 1 file changed, 2 insertions(+)

diff --git a/init/reboot.cpp b/init/reboot.cpp
index a1eecbf..c47483b 100644
--- a/init/reboot.cpp
+++ b/init/reboot.cpp
@@ -992,6 +992,7 @@ static void DoReboot(unsigned int cmd, const std::string& reason,
     if (!is_thermal_shutdown) std::this_thread::sleep_for(100ms);
 
     // Reboot regardless of umount status. If umount fails, fsck after reboot will fix it.
+#if 0
     std::string data_fs_type = GetDataFsType();
     if (!data_fs_type.empty()) {
         // Do not delete: Critical log for reboot_fs_integrity_test.
@@ -1020,6 +1021,7 @@ static void DoReboot(unsigned int cmd, const std::string& reason,
             LOG(ERROR) << "Unknown /data fs type: " << data_fs_type;
         }
     }
+#endif
 
     LogShutdownTime(stat, t);
     RebootSystem(cmd, reboot_target, reason);
-- 
2.55.0

